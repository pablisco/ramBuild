apply plugin: 'java'

buildscript {
    repositories {
        maven {
            url uri('../repo')
        }
    }
    dependencies {
      classpath project(':plugin')//'com.pablisco.gradle:rambuild:0.1'
    }
}

apply plugin: 'com.pablisco.gradle.rambuild'
